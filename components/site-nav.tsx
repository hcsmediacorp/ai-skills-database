"use client"; import Link from "next/link"; import {Github,Menu,Moon,Sparkles,Sun,X} from "lucide-react"; import {useTheme} from "next-themes"; import {useState} from "react"; import {Button} from "@/components/ui/button";
const links=[{href:"/",label:"Browse"},{href:"/upload",label:"Upload"}]; export function SiteNav(){const[open,setOpen]=useState(false); const{theme,setTheme}=useTheme(); return <header className="sticky top-0 z-50 border-b bg-background/85 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3"><Link href="/" className="flex items-center gap-2 font-bold"><span className="rounded-xl bg-primary p-2 text-primary-foreground"><Sparkles size={18}/></span>AI Skills Database</Link><nav className="hidden items-center gap-1 md:flex" aria-label="Primary navigation">{links.map(l=><Button key={l.href} asChild variant="ghost"><Link href={l.href}>{l.label}</Link></Button>)}<Button asChild variant="ghost" size="icon" aria-label="GitHub"><a href="https://github.com" target="_blank" rel="noreferrer"><Github size={18}/></a></Button><Button variant="ghost" size="icon" aria-label="Toggle dark mode" onClick={()=>setTheme(theme==="dark"?"light":"dark")}><Sun className="hidden dark:block" size={18}/><Moon className="dark:hidden" size={18}/></Button></nav><Button className="md:hidden" variant="ghost" size="icon" onClick={()=>setOpen(!open)} aria-label="Open menu">{open?<X/>:<Menu/>}</Button></div>{open&&<div className="border-t px-4 py-3 md:hidden">{links.map(l=><Link key={l.href} className="block rounded-lg px-3 py-2 text-sm hover:bg-accent" href={l.href} onClick={()=>setOpen(false)}>{l.label}</Link>)}</div>}</header>}
