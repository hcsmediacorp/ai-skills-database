import Link from "next/link"; import {Download} from "lucide-react"; import {Badge} from "@/components/ui/badge"; import {Button} from "@/components/ui/button"; import {Card,CardContent,CardFooter,CardHeader,CardTitle} from "@/components/ui/card"; import type {Skill} from "@/lib/types";
export function SkillCard({skill}:{skill:Skill}){return <Card className="flex h-full flex-col transition hover:-translate-y-1 hover:border-primary/40"><CardHeader><CardTitle className="text-xl"><Link href={`/skills/${skill.slug}`} className="hover:text-primary">{skill.title}</Link></CardTitle></CardHeader><CardContent className="flex-1"><p className="line-clamp-3 text-sm text-muted-foreground">{skill.description}</p><div className="mt-4 flex flex-wrap gap-2">{skill.tags.slice(0,4).map(t=><Badge key={t}>{t}</Badge>)}</div></CardContent><CardFooter className="justify-between"><Button asChild variant="secondary"><Link href={`/skills/${skill.slug}`}>Details</Link></Button>{skill.raw_url&&<Button asChild><a href={skill.raw_url} download><Download size={16}/>Download</a></Button>}</CardFooter></Card>}
